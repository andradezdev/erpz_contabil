[project]
name = "erpz_contabil"
authors = [
    { name = "ERPZ", email = "dev@erpz.io"}
]
description = "ERPZ Contábil - Demonstrações Cont?beis Brasileiras (DRE 6.404/76, Balan?o), Mapeamento RFB e SPED ECD"
requires-python = ">=3.12"
readme = "README.md"
dynamic = ["version"]
dependencies = [
    "sped>=0.4.5"
]

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"
